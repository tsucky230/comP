name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node-version: [22.x, 24.x]

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache npm dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ matrix.node-version }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ matrix.node-version }}-

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            daemon/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run compile

      - name: Build Rust daemon
        run: npm run daemon:build

      - name: Run TypeScript tests
        run: npm test

      - name: Run Rust tests
        run: npm run daemon:test

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.x

      - name: Install dependencies
        run: npm ci

      - name: Lint Markdown
        run: npm run lint:md

  sast:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Install cargo-audit
        # WHY pinned: this exact drift (an unpinned `cargo install` picking up a
        # newer cargo-audit whose CLI had changed, breaking the next step) is
        # what broke this job before --file replaced --manifest-path above.
        run: cargo install cargo-audit --version 0.22.2 --locked

      - name: Audit Rust dependencies
        # WHY --file rather than --manifest-path: cargo-audit dropped --manifest-path
        # at some point (this job installs whatever is latest, unpinned), and now only
        # takes -f/--file pointing directly at the lockfile.
        run: cargo audit --file daemon/Cargo.lock

      - name: Run Clippy (Rust linter)
        run: cargo clippy --all --manifest-path daemon/Cargo.toml -- -D warnings
